services:
  rpg-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rpg-mcp
    restart: unless-stopped
    ports:
      - "8080:8080"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
